<!-- SPDX-FileCopyrightText: 2026 Evan Ruiz and Dir2Site Contributors -->
<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->
<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="using:dir2site.ViewModels"
        mc:Ignorable="d" d:DesignWidth="440" d:DesignHeight="180"
        x:Class="dir2site.Views.ConfirmView"
        x:DataType="vm:ConfirmViewModel"
        Width="440" SizeToContent="Height"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        Title="Confirm">

    <StackPanel Margin="16" Spacing="10">

        <TextBlock Text="{Binding Heading}" FontWeight="Bold" TextWrapping="Wrap"/>

        <TextBlock Text="{Binding Detail}" TextWrapping="Wrap"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,8,0,0">
            <Button Content="{Binding ConfirmText}" Command="{Binding ConfirmCommand}"
                    Background="#C0392B" Foreground="White"/>
            <!-- Default is Cancel: leaning on the safe answer for anything that can't be undone. -->
            <Button Content="Cancel" Command="{Binding CancelCommand}" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
